# Backend - Proyecto Web

Este es el backend básico de la aplicación usando Node.js y Express.

## Cómo ejecutar

```bash
npm install
npm start
```

El servidor se levantará por defecto en `http://localhost:3000`.
